apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis-service
spec:
  serviceName: redis
  replicas: 1
  selector:
    matchLabels:
      steamscraper.io/name: redis
  template:
    metadata:
      labels:
        steamscraper.io/name: redis
    spec:
      containers:
      - name: redis
        image: redis:latest
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis-service
spec:
  clusterIP: None
  selector:
    steamscraper.io/name: redis-server
  ports:
    - name: redis
      port: 6379